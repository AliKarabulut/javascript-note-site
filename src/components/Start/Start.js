import styles from "./Start.module.css";

const Start = () => {
  return (
    <div className={styles.startWrapper}>
      <p className={styles.startP}>Javascript Notlarım</p>
    </div>
  );
};

export default Start;
